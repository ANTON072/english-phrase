{
  "name": "sync",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "sync": "tsx --env-file=../../.env src/index.ts",
    "sync:ci": "tsx src/index.ts",
    "test": "vitest run"
  },
  "dependencies": {
    "@english-phrase/db": "workspace:*",
    "@notionhq/client": "^2.0.0"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "tsx": "^4.0.0",
    "typescript": "catalog:",
    "vitest": "^3.0.0"
  }
}
