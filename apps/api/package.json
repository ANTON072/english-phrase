{
  "name": "api",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev --port 8787",
    "deploy": "wrangler deploy --env production",
    "test": "vitest run"
  },
  "dependencies": {
    "@english-phrase/db": "workspace:*",
    "@english-phrase/types": "workspace:*",
    "drizzle-orm": "catalog:",
    "hono": "^4.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.0.0",
    "typescript": "catalog:",
    "vitest": "^3.0.0"
  }
}
