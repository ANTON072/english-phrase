{
  "name": "english-phrase",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "sync": "pnpm --filter sync run sync",
    "test": "pnpm --filter sync run test",
    "db:generate": "pnpm --filter @english-phrase/db run generate",
    "db:studio": "pnpm --filter @english-phrase/db run studio",
    "db:migrate": "wrangler d1 execute english-phrase-db --remote --file=packages/db/migrations/0000_fluffy_impossible_man.sql"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["esbuild", "sharp", "workerd"]
  }
}
